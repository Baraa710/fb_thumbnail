A function that returns the url of video thumbnail or of image in a post by providing ID of the facebook post

Uses facebook Graph API

To generate an access token: https://developers.facebook.com/docs/marketing-apis/overview/authentication/
